BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[InvoiceDetail] ADD [name] NVARCHAR(1000) NOT NULL CONSTRAINT [InvoiceDetail_name_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
