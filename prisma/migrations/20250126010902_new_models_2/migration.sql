BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Category] DROP CONSTRAINT [Category_parentCategory_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
